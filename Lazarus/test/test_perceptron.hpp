#ifndef LAZARUS_HPP
#define LAZARUS_HPP

/**
 * Lazarus core largely sits as a testbed for modules and
 * algorithms to ensure functionality. This will link to the
 * main executable if it is desired to be run
 */

class Lazarus {
  public:
    Lazarus() = default;
    ~Lazarus() = default;
    float test_perceptron();
  private:

};


#endif
